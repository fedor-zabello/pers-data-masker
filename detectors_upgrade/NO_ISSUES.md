# Типы ПД без проблем

Эти типы проходят все тест-кейсы на сервере 21 (эталон). Правки не требуются.

## Email — `EmailDetector`

Все 6 кейсов (`email_01`–`email_06`) проходят: нижний/верхний регистр, точки,
дефис, подчёркивание, цифры в имени.

## CVV — `CvvDetector`

Оба кейса (`cvv_01`, `cvv_02`) проходят: верхний и нижний регистр.

## ПИН — `PinDetector`

Оба кейса (`pin_01`, `pin_02`) проходят: верхний и нижний регистр.

## Место рождения — `BirthPlaceDetector`

Все 4 кейса (`birth_place_01`–`birth_place_04`) проходят: город, город+область,
село, деревня.

## Комплексные кейсы — `complex.yaml`

Все 12 кейсов (`complex_01`–`complex_12`) проходят на сервере 21.

## Примечание

Если при исправлении других детекторов (например, `FullNameDetector`) эти типы
начнут падать — это регрессия. Проверять после каждой правки:

```bash
cd blackBox && python3 test_runner.py --url http://161.104.16.21:8080/persmasker
```